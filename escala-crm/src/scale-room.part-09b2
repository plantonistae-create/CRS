  async adminDecision(request,id){const actor=await this.actor(request,{adminOnly:true});const row=this.ensureRequest(id);if(!["pending","pending_admin"].includes(row.status))throw new ScaleError("Esta solicitação já foi decidida ou ainda aguarda o outro médico.",409);const b=await this.readJson(request);const approve=!!b.approve,now=Date.now(),decisionNote=text(b.note,1000,"observação");if(approve){
